#include "insertionsort.h"

void InsertionSort:sort(vector<int> &v) {
  /*
  for (int i=1; i<v.size(); i++) {
    int elementToInsert = v[i];
    bool shiftElements = false;
    for (int j=0; j<i; j++) {
      if (elementToInsert < v[j]) {
        shiftElements = true;
      }
    }
  }
  */
}
