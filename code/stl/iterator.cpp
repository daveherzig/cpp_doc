#include <iostream>
#include <list>
using namespace std;

int main(int argc, char **argv) {
  list<int> container;
  for (int i=0; i<10; i++) {
    container.push_back(i);
  }

  // Iterator zum Durchlaufen aller Elemente
  
  return 0;
}

